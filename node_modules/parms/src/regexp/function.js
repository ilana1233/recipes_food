"use strict";

var prefix = 'function(?:\\s+\\w+)?\\s*\\(';
var block_comment = require('./block_comment');
var line_comment = require('./line_comment');
var argument_name = require('./argument_name');
var argument_list = '(?:' + block_comment + '|' + line_comment + '|' + argument_name + '|\\s+|,)*';
var suffix = '\\)';

exports = module.exports = prefix + '(' + argument_list + ')' + suffix;
